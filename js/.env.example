# Server
NODE_ENV=development
PORT=8000
HOST=0.0.0.0

# Database
PG_HOST=localhost
PG_PORT=5432
PG_USERNAME=postgres
PG_PASSWORD=postgres
PG_DB=patient_portal_dev

# API Gateway — optional; when set, added as a server in the Swagger UI
# GATEWAY_BASE_URL=https://your-gateway.example.com
